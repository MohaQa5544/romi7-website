fix(admin): make all dialogs scroll on short viewports

Previous fix relied on the outer overlay's overflow-y-auto, but
that doesn't reliably scroll across browsers when body overflow
is locked. Move the scroll inside the card itself with
max-h:calc(100dvh - 2rem) + overflow-y-auto. Applied to all four
dialogs: file upload, question, announcement, unit.
