fix(admin): add mobile back-to-site and logout buttons

On mobile the admin sidebar is hidden, which also hid the logout
button, and the "back to site" link was sm:inline (hidden below
640px). Added icon-only back and logout buttons to the header on
mobile so admins can exit the dashboard without rotating their
phone.
